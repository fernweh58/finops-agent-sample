# AWS credentials for Makefile — copy to .env and customize.
#
# Cross-account mode: AWS_PROFILE points to the cost account where
# all Lambda functions are deployed. The Payer account profile is
# configured in terraform.tfvars (management_account_profile).

AWS_PROFILE=cde-cost
AWS_REGION=us-east-1
